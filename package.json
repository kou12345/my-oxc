{
  "name": "my-eslint",
  "version": "1.0.0",
  "main": "index.js",
  "bin": {
    "my-oxc": "./bin/my-oxc.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "lint:external": "oxlint",
    "lint:external:fix": "oxlint --fix",
    "lint:strict": "oxlint --deny-warnings",
    "lint:external:strict": "oxlint --deny-warnings"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Custom Oxc configuration for linting external repositories",
  "type": "module",
  "devDependencies": {
    "oxlint": "^1.6.0",
    "typescript": "^5.8.3"
  }
}
